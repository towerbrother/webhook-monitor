{
  "$schema": "ralph-plan-v1",
  "meta": {
    "project": "webhook-monitor",
    "description": "Product Requirements Document for webhook-monitor implementation. Each feature is a discrete, verifiable unit of work.",
    "totalFeatures": 38,
    "methodology": "Ralph Wiggum Technique — bash loop + structured plan + one-task-per-iteration",
    "references": [
      "https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents",
      "https://ghuntley.com/ralph/",
      "https://github.com/ghuntley/how-to-ralph-wiggum"
    ]
  },
  "features": [
    {
      "id": "idempotency-key-extraction",
      "category": "functional",
      "dependencies": [],
      "description": "Extract X-Idempotency-Key header from ingest request and validate max 255 chars. Return 400 if oversized (do not truncate). Pass to Event.create. Catch Prisma P2002 unique constraint violation and return 409 Conflict with original eventId.",
      "steps": [
        "Read apps/api/src/routes/webhooks.ts to understand current ingest route",
        "Read X-Idempotency-Key header (take first value if array)",
        "Validate idempotency key length <= 255 chars using Zod, return 400 if oversized (do NOT truncate)",
        "Pass idempotencyKey to prisma event.create (omit if header absent to preserve NULL behavior)",
        "Wrap event.create in try/catch for PrismaClientKnownRequestError code P2002",
        "On P2002: query existing event by (projectId, idempotencyKey) and return 409 with { success: true, eventId, receivedAt, duplicate: true }",
        "Add/update tests: duplicate key same project -> 409; duplicate key different project -> 201; no key -> 201; 256-char key -> 400",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "POST with X-Idempotency-Key: abc -> 201, event has idempotencyKey='abc'",
        "Same request again -> 409, returns same eventId",
        "Same key, different project -> 201, new event",
        "No header -> 201, event.idempotencyKey is null",
        "256-character key -> 400 Bad Request"
      ],
      "passes": true
    },
    {
      "id": "zod-validation",
      "category": "functional",
      "dependencies": [],
      "description": "Add Zod schemas for endpointId path param validation (alphanumeric + underscore/dash only) and optional body shape. Validate idempotency key max length 255 via Zod. Return 400 on validation failures.",
      "steps": [
        "Import Zod into routes/webhooks.ts",
        "Define EndpointParamsSchema validating endpointId as non-empty string matching /^[a-zA-Z0-9_-]+$/",
        "Define IdempotencyKeySchema: optional string, max length 255",
        "Apply validation at route entry, return 400 with Zod error details on failure",
        "Add tests: empty endpointId -> 400; endpointId with SQL injection chars -> 400; oversized idempotency key (>255 chars) -> 400",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Invalid endpointId (empty string) -> 400",
        "endpointId with special chars '; DROP TABLE --' -> 400",
        "Oversized idempotency key (>255 chars) -> 400",
        "Valid params -> 201 (no regression)"
      ],
      "passes": true
    },
    {
      "id": "worker-integration-test",
      "category": "testing",
      "dependencies": [],
      "description": "Add worker integration test: enqueue real job -> verify DeliveryAttempt created and Event.status = DELIVERED. This is the remaining unchecked item from Step 3.",
      "steps": [
        "Create integration test that enqueues a real BullMQ job with a mock HTTP target",
        "Verify DeliveryAttempt record is created with correct fields",
        "Verify Event.status transitions to DELIVERED",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Enqueued job against mock 200 server -> Event.status = DELIVERED, DeliveryAttempt.success = true",
        "Enqueued job against mock 500 server -> Event.status = RETRYING, DeliveryAttempt.success = false"
      ],
      "passes": false
    },
    {
      "id": "pino-logger-worker",
      "category": "operational",
      "dependencies": [],
      "description": "Replace ad-hoc console.log logger in worker with Pino instance. Add LOG_LEVEL env var. Configure pino-pretty for dev. Ensure structured JSON output in production. NEVER log endpoint objects (may contain signingSecret).",
      "steps": [
        "Add pino and pino-pretty deps to apps/worker/package.json",
        "Add LOG_LEVEL env var (default 'info') to apps/worker/src/env.ts",
        "Create apps/worker/src/logger.ts with createLogger factory using Pino",
        "Configure Pino redact paths to never log sensitive fields: redact: ['*.signingSecret', 'endpoint.signingSecret']",
        "Replace all console.log/console.error in index.ts and processor.ts with Pino logger calls",
        "Include { service: 'worker' } base context in all log lines",
        "Verify no console.log calls remain in worker source (grep to confirm)",
        "Verify logger.info({ endpoint }) redacts signingSecret field (add test)",
        "Update processor tests to work with new logger interface",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Worker startup log emitted at info level as valid JSON (non-pretty mode)",
        "LOG_LEVEL=warn suppresses info logs",
        "Logging endpoint object redacts signingSecret field",
        "All existing processor tests pass with new logger"
      ],
      "passes": false
    },
    {
      "id": "correlation-id",
      "category": "functional",
      "dependencies": ["pino-logger-worker"],
      "description": "Add correlationId to WebhookDeliveryJobData. Pass Fastify request.id as correlationId when enqueuing. Log correlationId in all processor log calls. Default to job.id if correlationId is undefined (backward compat for old jobs).",
      "steps": [
        "Add correlationId: string to WebhookDeliveryJobData in packages/queue/src/index.ts",
        "Update enqueueWebhookDelivery caller in webhooks.ts to pass request.id as correlationId",
        "Update processor to extract correlationId from job data (fallback to job.id)",
        "Include correlationId in every processor log call alongside eventId and projectId",
        "Add LOG_LEVEL env var to apps/api/src/env.ts if not already present",
        "Update tests to verify correlationId propagation",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Processor completion log includes correlationId matching value set at ingestion",
        "Job without correlationId (old format) falls back to job.id"
      ],
      "passes": false
    },
    {
      "id": "event-list-endpoint",
      "category": "functional",
      "dependencies": [],
      "description": "Add GET /webhooks/:endpointId/events endpoint. Returns paginated event list with status, idempotencyKey, receivedAt. Tenant-scoped via projectId. Enforce max page size 100, default 50.",
      "steps": [
        "Add GET /webhooks/:endpointId/events handler in routes/webhooks.ts",
        "Implement cursor-based or page+limit pagination with MAX_PAGE_SIZE=100, default limit=50",
        "Validate limit param: if >100 return 400 'Page size exceeds maximum of 100'",
        "Validate endpointId param with Zod",
        "Scope query by projectId for multi-tenancy (filter WHERE projectId = req.project.id)",
        "Return events with id, status, idempotencyKey, receivedAt, headers (exclude body for list view)",
        "Add integration tests: list events for endpoint; empty endpoint -> empty array; cross-tenant -> 404; limit=101 -> 400",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "GET events for endpoint with 3 events -> returns 3 items with correct fields",
        "Pagination works (limit=1 returns 1 item with cursor/next info)",
        "limit=101 -> 400 with 'exceeds maximum' error",
        "Cross-tenant endpoint -> 404"
      ],
      "passes": false
    },
    {
      "id": "replay-endpoint",
      "category": "functional",
      "dependencies": ["event-list-endpoint"],
      "description": "Add POST /webhooks/:endpointId/events/:eventId/replay endpoint with rate limiting (10 replays/min per project). Re-enqueues FAILED events. Guards against replaying DELIVERED, PENDING, or RETRYING events. Logs replay action with correlationId.",
      "steps": [
        "Add POST /webhooks/:endpointId/events/:eventId/replay handler",
        "Apply rate limit: 10 replays per minute per projectId (use in-memory or Redis counter)",
        "Validate endpointId and eventId params with Zod",
        "Lookup Event by (id, projectId) and verify endpointId matches",
        "Guard: DELIVERED -> 200 { queued: false, message: 'Already delivered' }",
        "Guard: PENDING or RETRYING -> 200 { queued: false, message: 'Already in progress' }",
        "On FAILED: log replay action with { eventId, projectId, correlationId, action: 'replay' }",
        "On FAILED: update Event.status = PENDING, enqueue job, return 202 { queued: true, eventId }",
        "Add integration tests: replay FAILED -> 202; replay DELIVERED -> 200 no-op; unknown eventId -> 404; cross-tenant -> 404; 11th replay in 1 min -> 429",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "POST replay on FAILED event -> 202, Event.status = PENDING, job in queue",
        "POST replay on DELIVERED event -> 200, { queued: false }",
        "POST replay on nonexistent event -> 404",
        "Cross-project event -> 404",
        "11th replay within 1 minute -> 429 Too Many Requests"
      ],
      "passes": false
    },
    {
      "id": "rate-limiting",
      "category": "security",
      "dependencies": [],
      "description": "Register @fastify/rate-limit with Redis store, keyed by projectId (after auth). Default 100 req/60s per project. Unauthenticated routes: 10 req/60s per IP. Exclude /health and /metrics. Fail-closed by default (503 on Redis error), with RATE_LIMIT_FAIL_OPEN env opt-in.",
      "steps": [
        "Add @fastify/rate-limit to apps/api/package.json",
        "Add RATE_LIMIT_MAX (default 100), RATE_LIMIT_WINDOW_MS (default 60000), RATE_LIMIT_FAIL_OPEN (default false) to apps/api/src/env.ts",
        "Register plugin in buildApp AFTER authentication middleware",
        "Configure keyGenerator: (req) => req.project?.id ?? `unauthenticated:${req.ip}`",
        "Configure max for authenticated: 100; for unauthenticated: 10 (stricter)",
        "Configure Redis store for cross-replica consistency",
        "Exclude /health and /metrics routes from rate limiting",
        "Configure fail-closed: on Redis error, return 503 (unless RATE_LIMIT_FAIL_OPEN=true)",
        "If RATE_LIMIT_FAIL_OPEN=true: use in-memory fallback with reduced limit (10 req/min)",
        "Add tests: 101st request same project -> 429; different project -> 200; Redis down + fail-closed -> 503; Redis down + fail-open -> 200 with reduced limit; /health not limited",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "101st authenticated request from same project within window -> 429 with Retry-After header",
        "101st request from different project -> 200",
        "11th unauthenticated request from same IP -> 429",
        "/health -> 200, not rate-limited",
        "Redis unavailable + RATE_LIMIT_FAIL_OPEN=false -> 503",
        "Redis unavailable + RATE_LIMIT_FAIL_OPEN=true -> 200 (fallback to in-memory limit)"
      ],
      "passes": false
    },
    {
      "id": "signing-secret-migration",
      "category": "security",
      "dependencies": [],
      "description": "Add optional signingSecret field to WebhookEndpoint schema with @sensitive annotation. Run migration. Verify signingSecret NEVER appears in API responses, logs, or error messages. Create explicit DTO excluding signingSecret.",
      "steps": [
        "Add signingSecret String? @db.Text to WebhookEndpoint in packages/db/prisma/schema.prisma with comment '/// @sensitive - never expose in API responses or logs'",
        "Run pnpm --filter @repo/db migrate dev --name add_signing_secret",
        "Read generated migration SQL file, verify it only adds column (no DROP, no ALTER with data loss)",
        "Document rollback command in migration: 'ALTER TABLE WebhookEndpoint DROP COLUMN signingSecret'",
        "Create packages/shared/src/dto.ts with WebhookEndpointDTO type that explicitly excludes signingSecret",
        "Update all endpoint API responses to use Prisma select without signingSecret: select: { id: true, url: true, projectId: true, createdAt: true }",
        "Add automated test: GET /webhooks/:id response body must NOT contain signingSecret field (even if null)",
        "Add test: grep codebase for 'console.log.*endpoint' or 'logger.*endpoint' without redact -> must not exist",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Migration applies without error (idempotent)",
        "Existing endpoints have signingSecret = null",
        "GET /webhooks/:id response does NOT contain signingSecret field",
        "No regression in existing tests",
        "Logging endpoint object does NOT expose signingSecret (redacted by Pino)"
      ],
      "passes": false
    },
    {
      "id": "hmac-verification",
      "category": "security",
      "dependencies": ["signing-secret-migration"],
      "description": "Implement HMAC-SHA256 signature verification in ingest route. Add verifyWebhookSignature helper using crypto.timingSafeEqual. Configure Fastify raw body preservation with 1MB size limit. Return 413 if body exceeds limit.",
      "steps": [
        "Configure Fastify to preserve raw body with size limit: bodyLimit: 1048576 (1MB)",
        "Add request size limit handler: return 413 Payload Too Large if body exceeds 1MB",
        "Add verifyWebhookSignature(rawBody: Buffer, signatureHeader: string, secret: string): boolean to @repo/shared",
        "Implementation: compute HMAC-SHA256 of rawBody using secret, compare with signatureHeader using crypto.timingSafeEqual",
        "Ensure both buffers same length before timingSafeEqual (pad or reject if mismatch)",
        "In POST /webhooks/:endpointId: if endpoint.signingSecret exists, read x-hub-signature-256 header",
        "Call verifyWebhookSignature, return 401 Unauthorized on failure (before event creation)",
        "Unit test verifyWebhookSignature: valid -> true; tampered body -> false; wrong secret -> false; signature 1 bit flipped -> false",
        "Integration tests: signed correct -> 201; wrong secret -> 401; no header with secret set -> 401; no secret on endpoint -> 201; 2MB body -> 413",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Valid HMAC-SHA256 signature -> 201",
        "Tampered body (1 bit changed) -> 401",
        "Wrong secret -> 401",
        "No signature header, endpoint has secret -> 401",
        "No signature header, endpoint has no secret -> 201",
        "2MB request body -> 413 Payload Too Large"
      ],
      "passes": false
    },
    {
      "id": "prometheus-metrics",
      "category": "operational",
      "dependencies": [],
      "description": "Add Prometheus-compatible metrics using prom-client. Instrument: events_received_total, delivery_attempts_total, delivery_duration_ms, queue_depth, events_status_total. Expose /metrics on API with optional Bearer token auth. Worker exposes on internal port (default 9091).",
      "steps": [
        "Add prom-client to apps/api/package.json and apps/worker/package.json",
        "Add METRICS_AUTH_TOKEN env var (optional) to apps/api/src/env.ts",
        "Create packages/shared/src/metrics.ts with metric definitions and shared registry",
        "Register GET /metrics in buildApp before other routes; exclude from project auth + rate limiting",
        "If METRICS_AUTH_TOKEN is set: require Authorization: Bearer <token> header, return 401 if missing/wrong",
        "If METRICS_AUTH_TOKEN not set: document in README that /metrics should be firewalled",
        "Increment webhook_events_received_total in ingest route on 201",
        "Record webhook_delivery_duration_ms and webhook_delivery_attempts_total in processor",
        "Add minimal HTTP server in worker on METRICS_PORT (env-configurable, default 9091) serving GET /metrics",
        "Add tests: GET /metrics with valid token -> 200; wrong token -> 401; no token required if env unset -> 200; Content-Type text/plain",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "After one ingest, webhook_events_received_total count = 1",
        "After one delivery, webhook_delivery_attempts_total incremented",
        "GET /metrics returns valid Prometheus text format",
        "GET /metrics with METRICS_AUTH_TOKEN set + wrong token -> 401",
        "GET /metrics with METRICS_AUTH_TOKEN set + correct token -> 200"
      ],
      "passes": false
    },
    {
      "id": "dockerfiles",
      "category": "infrastructure",
      "dependencies": [],
      "description": "Create production-ready multi-stage Dockerfiles for api, worker, and web apps. Node 24 Alpine base. Prisma client pre-generated. Run as non-root. Add .dockerignore. Verify no secrets baked into images using automated check.",
      "steps": [
        "Create apps/api/Dockerfile: multi-stage (builder installs deps + generates Prisma + builds; runtime copies artifacts only)",
        "API entrypoint runs prisma migrate deploy before starting Node",
        "Create apps/worker/Dockerfile: same multi-stage pattern, no HTTP",
        "Create apps/web/Dockerfile: set output standalone in next.config.ts, copy .next/standalone",
        "Add .dockerignore at monorepo root: exclude .git, node_modules, .next, dist, test files, .env, .env.*, *.log",
        "Verify docker build succeeds for all three apps",
        "Add automated check: docker history <image> | grep -iE 'password|secret|DATABASE_URL|api.?key' -> must be empty",
        "Add test: inspect final image filesystem (docker run --rm <image> ls -la), assert no .env files present",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "docker build succeeds for all three apps without errors",
        "Images run as non-root (USER node)",
        "docker history <image> contains no environment variables with secrets",
        "No .env files present in final image filesystem"
      ],
      "passes": false
    },
    {
      "id": "docker-compose",
      "category": "infrastructure",
      "dependencies": ["dockerfiles"],
      "description": "Update docker-compose.yml to add api, worker, and web services with depends_on (postgres + redis healthchecks). Add .env.example documenting all required env vars.",
      "steps": [
        "Add api, worker, web services to docker-compose.yml with proper depends_on and healthchecks",
        "Configure environment variables to reference .env file",
        "Create .env.example at repo root documenting all env vars for all three apps",
        "Test docker-compose up brings all services to healthy state",
        "Verify curl http://localhost:3001/health returns { status: ok }",
        "Verify API container with missing env var exits with non-zero code",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "docker-compose up brings all services to healthy state",
        "curl http://localhost:3001/health -> { status: ok }",
        "API container with missing DATABASE_URL -> exits non-zero"
      ],
      "passes": false
    },
    {
      "id": "load-test",
      "category": "validation",
      "dependencies": ["docker-compose"],
      "description": "Create k6 load test script: ramp to 100 VU, sustain 5 min, assert p95 < 200ms and error rate < 1%. Monitor memory and CPU usage. Add 1-hour sustained load test to catch memory leaks.",
      "steps": [
        "Create scripts/load-test.js (k6 script) with ramp-up and sustained load phases",
        "Configure thresholds: p95 < 200ms, error rate < 1%, memory < 1GB per service, CPU < 80%",
        "Create scripts/load-test-sustained.js: 1 hour continuous load at 50 VU",
        "Add memory leak detection: sample memory usage every 5 minutes, assert < 10% growth over 1 hour",
        "Document how to run: k6 run scripts/load-test.js and k6 run scripts/load-test-sustained.js",
        "Run load test against docker-compose stack and verify thresholds pass",
        "Run sustained load test and verify no memory leaks",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "p95 ingest latency < 200ms under load",
        "Error rate < 1% under sustained load",
        "Memory usage stable over 1 hour (< 10% growth)",
        "CPU usage < 80% under sustained load"
      ],
      "passes": false
    },
    {
      "id": "chaos-tests",
      "category": "validation",
      "dependencies": ["docker-compose"],
      "description": "Create chaos test scripts with data integrity verification: Redis disconnect mid-delivery, PostgreSQL disconnect mid-write, worker crash mid-job. Verify ALL events accounted for (no data loss) and graceful recovery.",
      "steps": [
        "Before chaos tests: insert 100 known test events with unique IDs, record count",
        "Create scripts/chaos-redis.sh: start load, stop Redis, verify graceful degradation, restart, verify recovery",
        "Create scripts/chaos-db.sh: stop Postgres, verify API returns 503 (not 500), restart, verify recovery",
        "Create scripts/chaos-worker.sh: kill worker process mid-job, restart, verify job continues",
        "After each chaos test: query Event table count, assert equals sent count (no events lost)",
        "After recovery: assert ALL events have status DELIVERED or FAILED (none stuck in PENDING)",
        "Verify delivery queue backlog drains within SLA after outage recovery",
        "Check DeliveryAttempt integrity: every DELIVERED event has at least one successful attempt",
        "Document findings and SLA thresholds in docs/infrastructure.md",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "All 100 test events accounted for after Redis restart (no data loss)",
        "All events either DELIVERED or FAILED after recovery (none stuck)",
        "Worker gracefully reconnects after Redis restart",
        "Ingest returns 503 (not 500) when DB is down",
        "Every DELIVERED event has corresponding successful DeliveryAttempt"
      ],
      "passes": false
    },
    {
      "id": "dashboard-scaffolding",
      "category": "frontend",
      "dependencies": [],
      "description": "Scaffold the webhook management dashboard in apps/web. Set up project layout, navigation shell, API client for the backend, and authentication context. Install Playwright for E2E testing.",
      "steps": [
        "Install a styling solution (e.g., Tailwind CSS) and any UI primitives needed (e.g., shadcn/ui or similar)",
        "Create app layout with sidebar navigation: Dashboard, Endpoints, Events",
        "Create a typed API client module (src/lib/api.ts) that calls the Fastify backend with X-Project-Key auth",
        "Add project selection or API key configuration (settings page or env-based for MVP)",
        "Install @playwright/test as dev dependency in apps/web",
        "Create playwright.config.ts with webServer config pointing at Next.js dev server",
        "Add test scripts to apps/web/package.json: test:e2e, test:e2e:ui",
        "Create a smoke E2E test: navigate to dashboard -> verify layout renders",
        "Run npx playwright test to verify setup works",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Next.js dev server starts without errors",
        "Dashboard layout renders with navigation sidebar",
        "Playwright smoke test passes: page loads, navigation visible",
        "API client module exports typed methods for all backend endpoints"
      ],
      "passes": false
    },
    {
      "id": "endpoint-list-page",
      "category": "frontend",
      "dependencies": ["dashboard-scaffolding"],
      "description": "Build the Endpoints list page. Displays all webhook endpoints for the current project with their URL, created date, and event count. Links to per-endpoint event views.",
      "steps": [
        "Create app/endpoints/page.tsx — server component fetching endpoint list from API",
        "Display endpoints in a table: name/ID, URL, created date, event count badge",
        "Each row links to /endpoints/[endpointId]/events",
        "Handle loading state and empty state (no endpoints configured)",
        "Handle API errors with user-friendly error boundary or message",
        "Add Playwright E2E test: verify endpoint list renders with test data",
        "Run pnpm test and npx playwright test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Endpoints page renders table with endpoint data from API",
        "Empty state shown when no endpoints exist",
        "Each endpoint row links to its event list page",
        "Playwright test: navigate to /endpoints -> table visible with correct columns"
      ],
      "passes": false
    },
    {
      "id": "event-list-page",
      "category": "frontend",
      "dependencies": ["dashboard-scaffolding", "event-list-endpoint"],
      "description": "Build the Event list page for a specific endpoint. Displays paginated events with status badges (PENDING/DELIVERED/FAILED/RETRYING), idempotency key, and receivedAt timestamp. Includes replay button for FAILED events.",
      "steps": [
        "Create app/endpoints/[endpointId]/events/page.tsx",
        "Fetch events from GET /webhooks/:endpointId/events with pagination",
        "Display events in a table: eventId, status badge (color-coded), idempotencyKey, receivedAt",
        "Add status badge component with distinct colors: PENDING (yellow), DELIVERED (green), FAILED (red), RETRYING (orange)",
        "Implement pagination controls (next/prev or infinite scroll)",
        "Add Replay button on FAILED event rows — calls POST .../replay, updates status optimistically",
        "Show confirmation dialog before replay",
        "Handle replay success (status changes to PENDING) and replay no-op (already delivered) responses",
        "Add Playwright E2E tests: event list renders, status badges display correct colors, replay button triggers API call",
        "Run pnpm test and npx playwright test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Event list page renders with events from the API",
        "Status badges show correct colors for each EventStatus",
        "Pagination controls work (navigate between pages)",
        "Replay button visible only on FAILED events",
        "Clicking Replay on FAILED event -> confirmation dialog -> API call -> status updates to PENDING",
        "Playwright test: full replay flow from event list to status update"
      ],
      "passes": false
    },
    {
      "id": "event-detail-page",
      "category": "frontend",
      "dependencies": ["event-list-page"],
      "description": "Build the Event detail page. Shows full event payload (headers + body), delivery attempt history with status codes and durations, and a replay action for failed events.",
      "steps": [
        "Create app/endpoints/[endpointId]/events/[eventId]/page.tsx",
        "Fetch event detail including headers, body, status, and delivery attempts",
        "Display event metadata: ID, status badge, idempotencyKey, receivedAt",
        "Display request headers and body in formatted JSON viewer (syntax-highlighted, collapsible)",
        "Display delivery attempt timeline: attemptNumber, requestedAt, respondedAt, statusCode, duration, success/failure",
        "Add Replay button (same behavior as event list page, with confirmation)",
        "Add Playwright E2E test: navigate to event detail, verify payload and attempts render",
        "Run pnpm test and npx playwright test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Event detail page renders with correct event data",
        "Headers and body displayed in formatted JSON",
        "Delivery attempt timeline shows all attempts with status codes and durations",
        "Replay button works for FAILED events",
        "Playwright test: navigate to event detail from event list -> all sections render"
      ],
      "passes": false
    },
    {
      "id": "dashboard-overview",
      "category": "frontend",
      "dependencies": ["dashboard-scaffolding"],
      "description": "Build the Dashboard overview page. Displays key metrics: total events, delivery success rate, events by status breakdown, and recent failed events requiring attention.",
      "steps": [
        "Create app/dashboard/page.tsx (or use app/page.tsx as dashboard root)",
        "Add summary cards: total events count, delivery success rate percentage, active endpoints count",
        "Add status breakdown: count of events per status (PENDING, DELIVERED, FAILED, RETRYING) with visual indicators",
        "Add recent failures list: last 10 FAILED events with endpoint name, event ID, last attempt timestamp, and quick replay action",
        "Fetch data from existing API endpoints (events list, metrics if available)",
        "Handle loading and error states",
        "Add Playwright E2E test: dashboard renders with summary cards and failure list",
        "Run pnpm test and npx playwright test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "Dashboard renders summary cards with numeric values from API",
        "Status breakdown shows correct counts per status",
        "Recent failures list shows FAILED events with replay action",
        "Playwright test: dashboard loads, summary cards and failure list visible"
      ],
      "passes": false
    },
    {
      "id": "security-audit",
      "category": "security",
      "dependencies": [
        "hmac-verification",
        "rate-limiting",
        "signing-secret-migration",
        "pino-logger-worker",
        "prometheus-metrics"
      ],
      "description": "Run comprehensive security audit: verify no secrets in logs/responses/images, test cross-tenant isolation on all endpoints, verify all oversized payloads rejected, check HMAC timing safety, validate rate limiting effectiveness.",
      "steps": [
        "Grep entire codebase for dangerous patterns: 'console.log.*endpoint', 'logger.info.*endpoint' without redact -> must not exist or use redacted logger",
        "Test cross-tenant isolation: for EVERY endpoint with projectId parameter, call with different project's X-Project-Key -> must return 404 (not 200 with wrong data)",
        "Endpoints to test: GET /webhooks/:endpointId/events, POST /webhooks/:endpointId/events/:eventId/replay, GET /webhooks/:endpointId (if exists)",
        "Test all string inputs with SQL injection payloads: '; DROP TABLE events--', '1' OR '1'='1', etc. -> all must return 400 (caught by Zod)",
        "Test all endpoints with oversized payloads: 2MB body -> 413; 256-char idempotency key -> 400; limit=1000 -> 400",
        "Test Docker images for secrets: run 'docker history api:latest | grep -iE \"password|secret|DATABASE_URL\"' -> must be empty",
        "Test HMAC timing safety: measure response times for 10,000 requests with invalid signatures, assert standard deviation < 5ms (no timing leak)",
        "Test rate limiting: send 101 requests in 60 seconds -> 101st must be 429; verify Retry-After header present",
        "Verify Prometheus /metrics endpoint: if METRICS_AUTH_TOKEN set, test with wrong token -> 401; no token -> 401",
        "Document all findings in docs/security-audit.md with pass/fail for each check",
        "Run pnpm test to verify all tests pass",
        "Commit with descriptive message"
      ],
      "gatewayTests": [
        "No console.log or unredacted logger calls with endpoint objects in codebase",
        "All cross-tenant API calls return 404 (tested: events list, replay, endpoint detail)",
        "All SQL injection attempts caught by validation (return 400)",
        "All oversized payloads rejected (2MB body -> 413, 256-char idempotency -> 400, limit=1000 -> 400)",
        "Docker images contain no secrets (docker history check passes)",
        "HMAC verification has no timing leak (std dev < 5ms)",
        "Rate limiting works (101st request -> 429 with Retry-After)",
        "Prometheus /metrics properly protected (wrong token -> 401)"
      ],
      "passes": false
    }
  ]
}
